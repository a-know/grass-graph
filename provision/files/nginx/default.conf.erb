server {
    listen       80 default_server;
    server_name  grass-graph.moshimo.works;

    access_log /var/log/nginx/grass-graph.moshimo.works.access.log ltsv;
    error_log  /var/log/nginx/grass-graph.moshimo.works.error.log;

    add_header Strict-Transport-Security "max-age=2592000; includeSubdomains";

    if ($request_method !~ ^(GET)$ ) {
        rewrite ^(.*)$ https://grass-graph.moshimo.works/ redirect;
    }

    location ~ /(jmx-console|GponForm|wls-wsat|webdav|muieblackcat|manager|cgi-bin|jbossass|shopdb|dbadmin|2phpmyadmin|mysql|PMA|myadmin|pma|myadmin|phpmyAdmin|database|program|phppma|phpmy|MyAdmin|administrator|splas-boomerang) {
        rewrite ^(.*)$ https://grass-graph.moshimo.works/ redirect;
    }

    location ~ .*\.php {
        rewrite ^(.*)$ https://grass-graph.moshimo.works/ redirect;
    }

    location ~ .*\.xml {
        rewrite ^(.*)$ https://grass-graph.moshimo.works/ redirect;
    }

    location ~ .*\.asp {
        rewrite ^(.*)$ https://grass-graph.moshimo.works/ redirect;
    }

    location ~ /user/register {
        rewrite ^(.*)$ https://grass-graph.moshimo.works/ redirect;
    }

    location ~ /images/grass-graph-logo.png {
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_cache zone1;
        add_header X-Nginx-Cache $upstream_cache_status;
        root   /var/www/grass-graph/app/public;
    }

    location ~ /images/.+\.png {
        proxy_pass   http://localhost:8080;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $http_host;
        proxy_cache zone1;
        proxy_redirect off;
        add_header X-Nginx-Cache $upstream_cache_status;
    }

    location ~ /(css|images|js|fonts|plugins) {
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_cache zone1;
        add_header X-Nginx-Cache $upstream_cache_status;
        root   /var/www/grass-graph/app/public;
    }

    location ~ .*\.(json|js|html|less|css|eot|svg|ttf|woff|otf|scss|txt|jpg|png|gif|map|ico) {
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_cache zone1;
        add_header X-Nginx-Cache $upstream_cache_status;
        root   /var/www/grass-graph/app/public;
    }

    location / {
        if ($http_referer ~* (localhost|127.0.0.1) ){
            return 403;
            break;
        }
        root   /var/www/grass-graph/app;
        proxy_pass   http://localhost:8080;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $http_host;
        proxy_cache zone1;
        proxy_redirect off;
        add_header X-Nginx-Cache $upstream_cache_status;
    }
}
